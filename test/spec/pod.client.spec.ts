import { PODClient } from "../../src/pod.client";

describe("POD Http client especification", () => {
  let podClient: PODClient;

  describe("1. Authentication method", () => {
    test("Expect POD Http client to be defined.", () => {
      expect(podClient).toBeDefined();
    });
  });

  describe("2. Create user method", () => {
    test("Expect POD Http client to be defined.", () => {
      expect(podClient).toBeDefined();
    });
  });

  describe("3. Get all assets method", () => {
    test("Expect POD Http client to be defined.", () => {
      expect(podClient).toBeDefined();
    });
  });

  describe("4. Get number of active, inactive and suspended assets method", () => {
    test("Expect POD Http client to be defined.", () => {
      expect(podClient).toBeDefined();
    });
  });
});
