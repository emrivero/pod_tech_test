/**
 * Pod client options
 * @typedef {Object} PODClientOptions
 * @param {string} username - The username
 * @param {string} password - The password
 * @param {string} host - The host url
 */
export type PODClientOptions = {
  username: string;
  password: string;
  host: string;
};
